# Equity Builders — Web App (`apps/web`)

This folder contains the **Next.js App Router** application (UI + route handlers + server actions).

For project overview and run instructions, see the repository root `README.md`.
