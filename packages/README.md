# Packages

This directory is reserved for shared packages as the platform scales:

- `db`: database access + migrations + typed query layer
- `contracts`: shared API contracts (request/response schemas)
- `ui`: shared UI primitives (design system)
- `utils`: shared utilities (dates, ids, validation)

