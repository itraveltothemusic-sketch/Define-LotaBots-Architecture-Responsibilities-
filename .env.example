# Equity Builders — local development configuration
# Copy to `.env.local` inside `apps/web/` (recommended) or set as environment variables.
#
# SECURITY: Never commit real secrets. Use the Cursor secrets dashboard for Cloud Agents.

# Postgres (Neon) connection string.
# Example: postgres://USER:PASSWORD@HOST:5432/DB?sslmode=require
DATABASE_URL=""

# Auth.js / NextAuth secret (generate a strong random value).
# Example generation: `openssl rand -base64 48`
AUTH_SECRET=""

# NextAuth base URL (recommended in production; helpful for absolute URL generation).
NEXTAUTH_URL="http://localhost:3000"

# Public app URL (used for callbacks, absolute URLs, etc.)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

